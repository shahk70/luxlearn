# LuxLearn environment configuration.
# Copy this file to .env and fill in your values. .env is git-ignored.

# WeatherAPI.com API key(s) for live sunrise/sunset + cloud-cover data.
# Optional — without keys the app uses keyless Open-Meteo, which works out
# of the box. Set your own keys (free at https://www.weatherapi.com/) to
# also get the resolved city name via WeatherAPI.
#
# WEATHERAPI_PRIVATE_KEYS (optional, keep in your local .env only):
#   Your own key(s), tried FIRST on every request, in order. Comma-separated.
#
# WEATHERAPI_KEYS (optional):
#   Extra keys (env keys come first).
#   Comma-separated; one is picked at random per request and a rejected key
#   (401/403/429 quota/blocked) retries with a different unused key.
# WEATHERAPI_KEYS=your-extra-key-1,your-extra-key-2
# WEATHERAPI_PRIVATE_KEYS=your-own-key-1,your-own-key-2

# Legacy single-key variable — still accepted.
# WEATHERAPI_KEY=your-key
